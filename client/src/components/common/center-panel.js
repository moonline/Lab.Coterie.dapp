import React from 'react';

const CenterPanel = ({ icon, children }) => (
	<div className="d-flex h-100 w-100 align-items-center justify-content-center">{children}</div>
);

export default CenterPanel;
